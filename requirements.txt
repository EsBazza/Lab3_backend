django==5.2.5
djangorestframework==3.15.0  # For rest_framework
django-cors-headers==4.4.0  # For corsheaders
gunicorn==23.0.0  # Production WSGI server for Render
dj-database-url==2.4.0  # For parsing DATABASE_URL on Render
psycopg2-binary==2.9.9  # For PostgreSQL (if using Render's DB)
whitenoise==6.6.0  # Optional: Compresses and serves static files efficiently
waitress==3.0.0  # Optional: For local testing on Windows (not needed on Render)